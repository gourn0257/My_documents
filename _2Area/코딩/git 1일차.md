```
// 먼저 git을 사용할 파일을 고른 후 유저의 정보를 입력
git config --global user.email "이메일주소"
git config --global user.name "이름"



// vs code로 git을 사용할 파일을 열고 새 터미널을 열고 이렇게 쓴다
git init
// 이제 git이 이 파일을 감시한다



// 
git add 파일이름
git commit -m "메모"























```